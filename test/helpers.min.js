function testTagsInput(a, b, c) {
    if (typeof b === "function") {
        c = b;
        b = undefined
    }
    describe(a + " (options: " + JSON.stringify(b, function (d, e) {
            return (typeof e === "function") ? e.toString() : e
        }) + ")", function () {
        beforeEach(function () {
            this.$sandbox = $("<div />").appendTo($("body"));
            this.$element = $(a).appendTo(this.$sandbox);
            this.$element.tagsinput(b);
            this.$tagsinput = $(".bootstrap-tagsinput", this.$sandbox);
            this.$tagsinput_input = $("input", this.$tagsinput)
        });
        afterEach(function () {
            this.$element.tagsinput("destroy");
            this.$sandbox.remove();
            delete this.$tagsinput_input;
            delete this.$tagsinput;
            delete this.$sandbox;
            delete this.$element
        });
        c()
    })
}
function hasFocus(a) {
    return a.get(0) === document.activeElement
};